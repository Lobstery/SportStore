import React from 'react';

import Container from '../../components/Container';

const Contacts = () => {
    return (
        <Container>
            Contacts
        </Container>
    );
};

export default Contacts;
